# yaml-language-server: $schema=https://go.fischerscode.com/serverpod-schema

### Information about a cluster of servers.
class: ClusterInfo
fields:
  ### List of servers in the cluster.
  servers: List<ClusterServerInfo>
